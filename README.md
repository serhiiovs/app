# App
App

How to use docker-compose:

1. To build images with cron service use:
```bash
docker-compose --profile cron build
```

2. To run all services use:
```bash
docker-compose --profile cron up
```

